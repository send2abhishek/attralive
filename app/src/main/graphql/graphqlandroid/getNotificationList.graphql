query getNotificationList(
$userId:String!){
getUserNotification_Q(userId:$userId)
  {
    status
    message
    notifications
  {
       postType
       postId
       ownerId
       action
       userId
       userName
       time
       readStatus
       postMessage
  }
  }
}

