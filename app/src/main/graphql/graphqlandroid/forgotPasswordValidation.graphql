mutation ForgotPasswordValidation(
$email: String!
$otp: String!
$newPassword: String!
$confirmPassword: String!){
forgotPasswordValidation_M(email:$email,OTP:$otp,newPassword:$newPassword,confirmPassword:$confirmPassword){
status,
message
}
}