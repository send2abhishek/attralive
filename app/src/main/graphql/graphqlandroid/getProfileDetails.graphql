query getProfileDetails(
$userId:String!
){
getProfileDetails_Q(userId:$userId){
name,
empId,
status,
email,
message,
profileImagePath,
designation,
location,
mobileNumber,
bu,
roles,
userQRCodeLink
}
}