mutation LikeDislikeAnswer(
$answerId:ID!,
$userId:String!,
$like_dislike:String!
){
  like_dislike_answer(answerId:$answerId,
  userId:$userId,like_dislike:$like_dislike)
  {
    id
    unlikes_count
    likes_count
  }
}