mutation ChangePassword(
$userId: String!
$currentPassword: String!
$newPassword: String!){
changePassword_M(userId:$userId,currentPassword:$currentPassword,newPassword:$newPassword){
status,
message
}
}